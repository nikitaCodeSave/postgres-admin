# Slash Commands - PostgreSQL Admin Dashboard

Набор slash команд для управления документацией и workflow проекта.

## 📚 Обзор команд

### Документация и планирование

| Команда | Описание | Использование |
|---------|----------|---------------|
| `/add-decision` | Создать Architecture Decision Record | Когда принимаешь важное архитектурное решение |
| `/new-feature` | Создать спецификацию фичи в backlog | Планируешь новую функциональность |
| `/create-bug` | Создать отчет о баге | Обнаружил баг |
| `/create-improvement` | Предложение по улучшению | Видишь как улучшить существующее |
| `/create-spec` | Спецификация реализованной фичи | После завершения фичи |

### Логирование и отслеживание

| Команда | Описание | Использование |
|---------|----------|---------------|
| `/log-change` | Добавить в CHANGELOG | После завершения изменения |
| `/log-dev` | Запись в dev journal | В конце дня / после работы |
| `/review-docs` | Обзор состояния документации | Периодически (раз в неделю/месяц) |

### Git и релизы

| Команда | Описание | Использование |
|---------|----------|---------------|
| `/git-commit` | Conventional commit | При создании коммита |
| `/create-release` | Подготовить релиз | Когда готов к новой версии |

## 🔄 Типичный Workflow

### 1. Планирование новой фичи

```bash
# Создаем спецификацию фичи в backlog
/new-feature

# Если есть архитектурное решение
/add-decision
```

### 2. Разработка

```bash
# Работаем над кодом...

# Создаем commits
/git-commit
```

### 3. После завершения фичи

```bash
# Обновляем CHANGELOG
/log-change

# Создаем детальную спецификацию
/create-spec

# Записываем в dev journal
/log-dev
```

### 4. Обнаружили баг

```bash
# Создаем bug report
/create-bug

# После исправления
/git-commit
/log-change
```

### 5. Улучшение существующего

```bash
# Предложение по улучшению
/create-improvement

# После реализации
/git-commit
/log-change
```

### 6. Подготовка релиза

```bash
# Проверяем состояние документации
/review-docs

# Создаем релиз
/create-release
```

## 📋 Детальное описание команд

### `/add-decision` - Architecture Decision Record

**Когда использовать**:
- Выбор технологического стека
- Решения о паттернах архитектуры
- Выбор библиотек для core функциональности
- Стратегии хранения данных

**Пример**:
```
/add-decision
> "Использование React Query для state management"
```

**Создает**: `docs/adr/NNNN-short-title.md`

---

### `/new-feature` - Спецификация новой фичи

**Когда использовать**:
- Планируешь добавить новую функциональность
- Фича достаточно большая для спецификации
- Нужно обсудить требования

**Пример**:
```
/new-feature database-browser
> Приоритет: P1
> Проблема: Нет способа просматривать данные в БД
> Решение: UI для просмотра таблиц и данных
```

**Создает**: `docs/backlog/features/P1-database-browser.md`

---

### `/create-bug` - Отчет о баге

**Когда использовать**:
- Обнаружил баг любой серьезности
- Нужно задокументировать проблему
- Требуется tracking исправления

**Пример**:
```
/create-bug
> Серьезность: HIGH
> Описание: "Migration rollback fails"
> Шаги воспроизведения: ...
```

**Создает**: `docs/backlog/bugs/HIGH-migration-rollback-fails.md`

---

### `/create-improvement` - Улучшение существующего

**Когда использовать**:
- Существующая функция работает, но может быть лучше
- Хочешь улучшить UX, performance, security
- НЕ новая функциональность

**Пример**:
```
/create-improvement
> Область: ui
> Название: "Add loading skeletons"
> Текущее состояние: Показывается spinner при загрузке
> Улучшение: Skeleton screens для лучшего UX
```

**Создает**: `docs/backlog/improvements/ui-add-loading-skeletons.md`

---

### `/create-spec` - Спецификация реализованной фичи

**Когда использовать**:
- ПОСЛЕ реализации фичи
- Нужна reference документация
- Хочешь задокументировать API, архитектуру

**Пример**:
```
/create-spec visual-alembic-ui
> Проблема: Нет UI для Alembic
> Решение: Timeline с apply/rollback кнопками
> Backend endpoints: GET /api/migrations/history, ...
> Frontend components: MigrationsPage, ...
```

**Создает**: `docs/specs/001-visual-alembic-ui.md`

---

### `/log-change` - Запись в CHANGELOG

**Когда использовать**:
- После завершения любого изменения
- Перед коммитом (или после)
- Всегда для features, fixes, breaking changes

**Пример**:
```
/log-change
> Тип: Added
> Описание: "Visual Alembic UI - timeline для миграций"
```

**Обновляет**: `docs/CHANGELOG.md` секцию `[Unreleased]`

---

### `/log-dev` - Development Journal

**Когда использовать**:
- В конце рабочего дня
- После интересного эксперимента
- Когда решил сложную проблему
- Для personal заметок

**Пример**:
```
/log-dev
> Работал над: "Интеграция React Query"
> Находки: "Кеширование сократило время загрузки с 500ms до 50ms"
> Коммиты: abc123, def456
> Сложность: 🟡
```

**Добавляет в**: `docs/dev-journal/YYYY-MM.md`

---

### `/review-docs` - Обзор документации

**Когда использовать**:
- Периодически (раз в неделю или месяц)
- Перед релизом
- Когда нужен общий обзор проекта
- Для выявления пробелов в документации

**Пример**:
```
/review-docs
```

**Выдает**: Comprehensive отчет с метриками, action items, recommendations

---

### `/git-commit` - Conventional Commit

**Когда использовать**:
- При создании любого коммита
- Хочешь следовать best practices
- Нужна помощь с форматированием

**Пример**:
```
/git-commit
> Type: feat
> Scope: backend
> Subject: "add AlembicService for migrations"
> Breaking changes: нет
```

**Создает коммит**: `feat(backend): add AlembicService for migrations`

---

### `/create-release` - Подготовка релиза

**Когда использовать**:
- Готов к новой версии
- Накопилось достаточно изменений
- Нужно создать git tag

**Пример**:
```
/create-release v0.2.0
> Тип изменений: Minor (новые features)
> Обновить package.json: да
> Создать git tag: да
> Push в remote: да
```

**Выполняет**:
- Обновляет CHANGELOG
- Создает commit и tag
- Опционально push и GitHub Release

## 🎯 Best Practices

### 1. Используй команды последовательно

```
Фича → ADR (если нужно) → Разработка → Commits → Spec → CHANGELOG → Dev Journal → Release
```

### 2. Не пропускай документацию

- Каждая фича → spec после реализации
- Каждый баг → bug report
- Каждое архитектурное решение → ADR

### 3. Пиши подробно в Dev Journal

- Это для тебя в будущем
- Записывай "почему", не только "что"
- Включай эмоции, проблемы, выводы

### 4. Регулярно делай /review-docs

- Выявляет пробелы
- Поддерживает документацию актуальной
- Помогает видеть общую картину

### 5. Следуй Conventional Commits

- Используй `/git-commit`
- Понятный git history = хорошая документация

## 🔍 Поиск нужной команды

**Хочу задокументировать архитектурное решение** → `/add-decision`
**Планирую новую фичу** → `/new-feature`
**Нашел баг** → `/create-bug`
**Хочу улучшить существующее** → `/create-improvement`
**Завершил фичу** → `/create-spec` + `/log-change`
**Сделал изменение** → `/git-commit` + `/log-change`
**Конец дня** → `/log-dev`
**Хочу overview** → `/review-docs`
**Готов к релизу** → `/create-release`

## 📁 Структура документации

```
docs/
├── adr/                      # ADR от /add-decision
├── backlog/
│   ├── features/             # От /new-feature
│   ├── bugs/                 # От /create-bug
│   └── improvements/         # От /create-improvement
├── specs/                    # От /create-spec
├── dev-journal/              # От /log-dev
├── architecture/             # Ручные доки
└── CHANGELOG.md              # От /log-change и /create-release
```

## 🚀 Быстрый старт

1. **Начинаешь новую фичу?**
   ```bash
   /new-feature
   /add-decision  # если есть архитектурное решение
   ```

2. **Работаешь над кодом?**
   ```bash
   /git-commit  # для коммитов
   ```

3. **Завершил фичу?**
   ```bash
   /create-spec
   /log-change
   /log-dev
   ```

4. **Нашел проблему?**
   ```bash
   /create-bug      # или /create-improvement если не баг
   ```

5. **Готов к релизу?**
   ```bash
   /review-docs
   /create-release
   ```

## 💡 Советы

- 🎯 **Используй argument hints**: Некоторые команды принимают аргументы
- 📝 **Пиши подробно**: Лучше больше информации чем меньше
- 🔄 **Будь последовательным**: Используй команды регулярно
- 📊 **Отслеживай прогресс**: `/review-docs` покажет общую картину
- 🚀 **Автоматизируй**: Команды экономят время и обеспечивают consistency

## 🔗 Связанные ресурсы

- [Официальная документация Claude Code](https://docs.claude.com/en/docs/claude-code/slash-commands)
- [Keep a Changelog](https://keepachangelog.com/)
- [Conventional Commits](https://www.conventionalcommits.org/)
- [Semantic Versioning](https://semver.org/)
- [MADR (ADR template)](https://adr.github.io/madr/)
