---
description: Подготовить новый релиз проекта
allowed-tools: Bash(date:*), Bash(git:*), Read, Edit, Write
argument-hint: [версия]
---

Помогаю подготовить новый релиз проекта по Semantic Versioning.

## Контекст

Релиз включает:
1. Перемещение items из [Unreleased] в новую версию в CHANGELOG
2. Создание git tag
3. Опционально: обновление version в package.json/pyproject.toml

## Semantic Versioning

**Формат**: `MAJOR.MINOR.PATCH` (например, 1.2.3)

- **MAJOR** - Breaking changes (несовместимые изменения API)
- **MINOR** - Новая функциональность (обратно совместимая)
- **PATCH** - Bug fixes (обратно совместимые)

## Текущее состояние

**Текущая ветка**: !`git branch --show-current`

**Последний tag**: !`git describe --tags --abbrev=0 2>/dev/null || echo "Нет tags"`

**Коммиты с последнего релиза**: !`git log --oneline $(git describe --tags --abbrev=0 2>/dev/null || git rev-list --max-parents=0 HEAD)..HEAD 2>/dev/null | head -10`

## Unreleased изменения

@docs/CHANGELOG.md

## Процесс создания релиза

1. **Определить версию** на основе типа изменений:
   - Есть breaking changes? → MAJOR
   - Есть новые features? → MINOR
   - Только bug fixes? → PATCH

2. **Спросить версию**:
   - Предложить версию на основе анализа
   - Дать пользователю подтвердить или изменить

3. **Обновить CHANGELOG**:
   - Переместить items из `[Unreleased]` в новую секцию
   - Формат: `## [X.Y.Z] - YYYY-MM-DD`
   - Сохранить пустую секцию `[Unreleased]` для будущих изменений
   - Обновить ссылки comparison в конце файла

4. **Спросить о дополнительных действиях**:
   - Обновить version в package.json? (если есть)
   - Обновить version в pyproject.toml? (если есть)
   - Создать git commit для версии?
   - Создать git tag?

5. **Выполнить действия** на основе ответов

## Пример обновления CHANGELOG

### До:
```markdown
## [Unreleased]

### Added
- **New Feature** - description

### Fixed
- **Bug Fix** - description
```

### После:
```markdown
## [Unreleased]

## [0.2.0] - 2025-10-27

### Added
- **New Feature** - description

### Fixed
- **Bug Fix** - description
```

## Git команды для релиза

```bash
# 1. Commit CHANGELOG изменений
git add docs/CHANGELOG.md [package.json] [pyproject.toml]
git commit -m "chore: release v0.2.0"

# 2. Создать annotated tag
git tag -a v0.2.0 -m "Release v0.2.0

Changelog:
- Added: New Feature
- Fixed: Bug Fix
"

# 3. Push изменения и tag
git push origin main
git push origin v0.2.0
```

## Pre-release checklist

Перед созданием релиза проверить:
- ✅ Все тесты проходят
- ✅ Нет uncommitted изменений
- ✅ CHANGELOG полностью описывает изменения
- ✅ README обновлен если нужно
- ✅ Документация актуальна
- ✅ Ветка main (или master)

## Важные вопросы

После определения версии спросить:
1. **Создать commit с изменениями?** (да/нет)
2. **Создать git tag?** (да/нет)
3. **Push в remote?** (да/нет)
4. **Создать GitHub Release?** (да/нет - использовать gh release create)

## GitHub Release

Если пользователь хочет GitHub Release:
```bash
gh release create v0.2.0 \
  --title "Release v0.2.0" \
  --notes "$(sed -n '/## \[0.2.0\]/,/## \[/p' docs/CHANGELOG.md | sed '1d;$d')"
```

## Важно

- 🏷️ Всегда использовать annotated tags (`-a`), не lightweight
- 📋 CHANGELOG должен быть полным перед релизом
- ✅ Проверить что все тесты проходят
- 🌿 Убедиться что на правильной ветке (main/master)
- 🚫 НЕ делать force push с тегами
- 📦 Для pre-release версий использовать: v0.2.0-alpha.1, v0.2.0-beta.1
