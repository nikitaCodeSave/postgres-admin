---
description: Создать отчет о баге в backlog
allowed-tools: Bash(date:*), Bash(ls:*), Bash(git:*), Read, Write
argument-hint: [severity] [краткое-описание]
---

Помогаю создать отчет о баге в backlog проекта.

## Контекст

Отчеты о багах хранятся в `docs/backlog/bugs/` с форматом имени: `[severity]-[short-description].md`

**Уровни серьезности**:
- **CRITICAL** - Система не работает, потеря данных, уязвимость безопасности
- **HIGH** - Основная функциональность сломана, затрагивает многих пользователей
- **MEDIUM** - Функция частично сломана, есть workaround
- **LOW** - Мелкая проблема, косметическая, edge case

## Существующие баги

!`ls -1 docs/backlog/bugs/*.md 2>/dev/null | grep -v template || echo "Нет открытых багов"`

## Текущая информация

Дата: !`date +%Y-%m-%d`
Текущий коммит: !`git rev-parse --short HEAD`
Ветка: !`git branch --show-current`

## Процесс создания отчета

1. **Задать вопросы**:
   - Серьезность бага? (CRITICAL / HIGH / MEDIUM / LOW)
   - Краткое описание (2-5 слов)
   - Фактическое поведение? (что происходит)
   - Ожидаемое поведение? (что должно происходить)
   - Шаги для воспроизведения?
   - Есть ли сообщения об ошибках или логи?
   - Детали окружения? (OS, браузер, версии)

2. **Создать файл отчета**:
   - Использовать шаблон: `docs/backlog/bugs/bug-template.md`
   - Имя файла: `docs/backlog/bugs/[SEVERITY]-[kebab-case-description].md`
   - Заполнить все секции на основе ответов
   - Установить статус "Open"
   - Установить сегодняшнюю дату как дату обнаружения

3. **После создания уточнить**:
   - Есть ли workaround для этого бага?
   - Есть ли идеи о возможной причине?
   - Нужно ли приоритизировать для немедленного исправления?

4. **Если CRITICAL или HIGH - напомнить**:
   - Рассмотреть добавление в текущий спринт/немедленную работу
   - Документировать в dev journal при исследовании
   - Обновить CHANGELOG когда исправлено
   - Возможно создать hotfix ветку

## Примеры хороших названий багов

✅ **Хорошие примеры**:
- `CRITICAL-database-connection-lost.md`
- `HIGH-migration-rollback-error.md`
- `MEDIUM-ui-button-misaligned.md`
- `LOW-typo-in-tooltip.md`

❌ **Плохие примеры**:
- `bug.md` - нет severity и описания
- `error.md` - слишком общее
- `HIGH-something-wrong.md` - неконкретное описание

## Критерии серьезности

**CRITICAL** 🔴:
- Система полностью не работает
- Потеря данных пользователей
- Критическая уязвимость безопасности
- Нет workaround
- Требует немедленного hotfix

**HIGH** 🟠:
- Основная функция не работает
- Затрагивает большинство пользователей
- Сложный workaround
- Блокирует работу

**MEDIUM** 🟡:
- Функция работает частично
- Есть простой workaround
- Затрагивает часть пользователей
- Некритично для работы

**LOW** 🟢:
- Косметические проблемы
- Edge cases
- Редкие сценарии
- Не влияет на функциональность

## Важно

- 🎯 Быть конкретным в описании
- 📋 Включить четкие шаги воспроизведения
- 📸 Приложить логи, скриншоты если есть
- ⚠️ Не занижать серьезность - лучше переоценить чем недооценить
- 💬 Если не уверен в серьезности - обсудим и скорректируем
