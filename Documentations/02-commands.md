# Руководство по Slash-командам (Commands)

## Что такое slash-команды?

Slash-команды — это пользовательские команды для быстрого выполнения часто используемых задач. Они вызываются через префикс `/` и могут содержать шаблоны, аргументы и даже выполнять bash команды.

**Преимущества:**
- ⚡ Быстрый доступ к частым задачам
- 🔄 Переиспользуемые шаблоны
- 📝 Стандартизация процессов в команде
- 🎯 Поддержка аргументов
- 🔗 Ссылки на файлы
- 💻 Выполнение bash команд

## Структура файлов

### Расположение

**Проектные команды** (для команды):
```
.claude/commands/command-name.md
```

**Пользовательские команды** (личные):
```
~/.claude/commands/command-name.md
```

### Namespace через подпапки

Организуйте команды в подпапки:
```
.claude/commands/
├── git/
│   ├── commit.md      → /commit (project:git)
│   └── review-pr.md   → /review-pr (project:git)
├── docs/
│   └── api.md         → /api (project:docs)
└── deploy.md          → /deploy (project)
```

**Приоритет:** Проектные > Пользовательские

## Формат файла

### Базовая структура

```markdown
Содержимое команды — промпт для Claude.
Может быть многострочным.
```

### С YAML frontmatter

```markdown
---
description: Краткое описание команды
allowed-tools: Bash(git add:*), Bash(git status:*)
argument-hint: [pr-number] [priority] [assignee]
model: claude-3-5-haiku-20241022
disable-model-invocation: false
---

Промпт команды с инструкциями для Claude.
```

## YAML параметры

### description
Описание команды, отображаемое в `/help` и автокомплите.

```yaml
description: Создать коммит по стандартам проекта
```

**Важно:** Обязательно для использования через инструмент `SlashCommand`.

### allowed-tools
Ограничение инструментов, которые может использовать команда.

```yaml
allowed-tools: Bash(git add:*), Bash(git status:*), Read, Write
```

**Примеры:**
- `Bash(git add:*)` — разрешить `git add` с любыми параметрами
- `Read, Write, Edit` — базовые операции с файлами
- `Bash(*), Read, Grep` — bash команды и чтение

### argument-hint
Подсказка по аргументам для автокомплита.

```yaml
argument-hint: [pr-number] [priority] [assignee]
```

Отображается как: `/review-pr [pr-number] [priority] [assignee]`

### model
Переопределить модель для команды.

```yaml
model: claude-3-5-haiku-20241022
```

**Использование:** для простых команд используйте Haiku для экономии.

### disable-model-invocation
Запретить Claude вызывать команду через инструмент `SlashCommand`.

```yaml
disable-model-invocation: true
```

## Работа с аргументами

### $ARGUMENTS — все аргументы

Захватывает все аргументы как одну строку.

**Команда:** `.claude/commands/fix-issue.md`
```markdown
---
description: Исправить проблему с указанным номером
argument-hint: [issue-number] [description]
---

Исправь проблему #$ARGUMENTS следуя нашим coding standards.
Создай коммит с правильным сообщением.
```

**Использование:**
```
/fix-issue 123 critical-bug
```

**Результат:**
```
Исправь проблему #123 critical-bug следуя нашим coding standards.
```

### $1, $2, $3 — позиционные аргументы

Доступ к отдельным аргументам по позиции.

**Команда:** `.claude/commands/git/review-pr.md`
```markdown
---
description: Провести ревью pull request
argument-hint: [pr-number] [priority] [assignee]
allowed-tools: Bash(gh pr:*), Read, Grep
---

Провести ревью PR #$1 с приоритетом $2.
После ревью назначить на $3.

Проверить:
1. Code quality
2. Tests
3. Security
4. Performance
```

**Использование:**
```
/review-pr 456 high alice
```

**Результат:**
```
Провести ревью PR #456 с приоритетом high.
После ревью назначить на alice.
```

## Выполнение Bash команд

Команды с префиксом `!` выполняются ДО передачи промпта Claude.

### Пример: Вставка git статуса

`.claude/commands/commit.md`
```markdown
---
description: Создать коммит по conventional commits
allowed-tools: Bash(git add:*), Bash(git commit:*), Bash(git status:*)
---

Текущий статус репозитория:
!`git status`

Последние коммиты:
!`git log --oneline -5`

Создай коммит message следуя Conventional Commits:
- feat: новая функциональность
- fix: исправление бага
- docs: изменения в документации
- style: форматирование
- refactor: рефакторинг
- test: добавление тестов
- chore: прочее

Проанализируй изменения и предложи commit message.
После согласования создай коммит.
```

**Результат:** Claude видит реальный вывод `git status` в промпте.

### Пример: Текущая ветка

`.claude/commands/git/sync.md`
```markdown
---
description: Синхронизировать текущую ветку с main
allowed-tools: Bash(git:*)
---

Текущая ветка: !`git branch --show-current`

Выполни:
1. git fetch origin
2. git rebase origin/main
3. Разреши конфликты если есть
4. git push --force-with-lease
```

## Ссылки на файлы

Используйте `@` для включения содержимого файлов.

### Пример: Ревью конкретного файла

`.claude/commands/review-file.md`
```markdown
---
description: Провести детальное ревью файла
argument-hint: [file-path]
---

Провести code review файла: @$1

Проверить:
- Code quality
- Security
- Performance
- Best practices
- Test coverage

Предоставить детальный анализ и рекомендации.
```

**Использование:**
```
/review-file src/auth/login.js
```

### Пример: Сравнение файлов

`.claude/commands/compare.md`
```markdown
---
description: Сравнить два файла
argument-hint: [file1] [file2]
---

Сравнить файлы:

**Файл 1:** @$1
**Файл 2:** @$2

Найти различия и объяснить изменения.
```

## Примеры команд

### 1. Создание коммита

`.claude/commands/commit.md`
```markdown
---
description: Создать conventional commit
allowed-tools: Bash(git:*), Read, Grep
---

Текущий статус:
!`git status --short`

Diff staged изменений:
!`git diff --cached`

Создай commit message по Conventional Commits:

**Формат:**
```
type(scope): subject

body

footer
```

**Types:**
- feat: новая функциональность
- fix: исправление бага
- docs: документация
- style: форматирование
- refactor: рефакторинг без изменения функциональности
- test: тесты
- chore: инфраструктура, зависимости

**Правила:**
1. Subject <= 50 символов
2. Body <= 72 символа на строку
3. Использовать imperative mood
4. Добавить breaking changes в footer если есть

После создания message выполни: git commit -m "message"
```

### 2. Создание PR

`.claude/commands/create-pr.md`
```markdown
---
description: Создать pull request
allowed-tools: Bash(git:*), Bash(gh:*), Read, Grep
---

Текущая ветка: !`git branch --show-current`
Коммиты для PR: !`git log origin/main..HEAD --oneline`

Создай pull request:

**Title:** Краткое описание (max 50 chars)

**Description:**
## Что изменено
- Bullet points изменений

## Зачем
Причина изменений

## Как тестировать
1. Шаги для проверки

## Screenshots (если UI)
[Screenshots если нужно]

## Checklist
- [ ] Тесты добавлены
- [ ] Документация обновлена
- [ ] Нет breaking changes (или описаны)

После формирования используй:
gh pr create --title "Title" --body "Description"
```

### 3. Генерация тестов

`.claude/commands/test.md`
```markdown
---
description: Создать тесты для файла
argument-hint: [file-path]
---

Файл для тестирования: @$1

Создай полное покрытие тестами:

**Типы тестов:**
1. **Happy path** — нормальное поведение
2. **Edge cases** — граничные условия
3. **Error handling** — обработка ошибок
4. **Integration** — взаимодействие с другими частями

**Структура:**
```javascript
describe('ComponentName', () => {
  describe('methodName', () => {
    it('should do X when Y', () => {
      // Arrange
      // Act
      // Assert
    });
  });
});
```

**Требования:**
- Используй AAA pattern
- Тесты независимы друг от друга
- Моки для внешних зависимостей
- Понятные названия тестов

Создай тестовый файл рядом с оригиналом.
```

### 4. Документация API

`.claude/commands/docs/api.md`
```markdown
---
description: Создать API документацию для endpoint
argument-hint: [endpoint-file]
---

Файл endpoint: @$1

Создай документацию в формате:

## `METHOD /path`

### Описание
Что делает endpoint

### Authentication
Требования к аутентификации

### Request

**Headers:**
```
Content-Type: application/json
Authorization: Bearer {token}
```

**Body:**
```json
{
  "field": "value"
}
```

**Parameters:**
| Name | Type | Required | Description |
|------|------|----------|-------------|
| field | string | yes | Description |

### Response

**Success (200):**
```json
{
  "data": {}
}
```

**Errors:**
- `400` — Bad Request
- `401` — Unauthorized
- `404` — Not Found
- `500` — Internal Server Error

### Example

```bash
curl -X POST https://api.example.com/endpoint \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer token" \
  -d '{"field":"value"}'
```

Сохрани в docs/api/endpoint.md
```

### 5. Рефакторинг

`.claude/commands/refactor.md`
```markdown
---
description: Рефакторинг кода с сохранением функциональности
argument-hint: [file-path]
---

Файл для рефакторинга: @$1

Проведи рефакторинг по принципам:

**1. SOLID**
- Single Responsibility
- Open/Closed
- Liskov Substitution
- Interface Segregation
- Dependency Inversion

**2. Clean Code**
- Понятные имена переменных/функций
- Функции делают одну вещь
- Нет дублирования (DRY)
- Комментарии только где необходимо

**3. Паттерны**
- Extract Function
- Extract Variable
- Replace Magic Numbers
- Simplify Conditional Logic

**Процесс:**
1. Убедись что есть тесты
2. Маленькие шаги
3. Тесты проходят после каждого шага
4. Коммит после каждого изменения

Начни с самых простых улучшений.
```

### 6. SQL миграция

`.claude/commands/migration.md`
```markdown
---
description: Создать SQL миграцию
argument-hint: [migration-name]
---

Создай миграцию: $1

**Требования:**
1. Up и Down миграции
2. Транзакционная безопасность
3. Откат должен работать
4. Проверка существования объектов

**Шаблон:**
```sql
-- Migration: $1
-- Created: [timestamp]

BEGIN;

-- UP Migration
CREATE TABLE IF NOT EXISTS table_name (
    id SERIAL PRIMARY KEY,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX IF NOT EXISTS idx_name ON table_name(column);

COMMIT;
```

**Rollback:**
```sql
BEGIN;

-- DOWN Migration
DROP INDEX IF EXISTS idx_name;
DROP TABLE IF EXISTS table_name;

COMMIT;
```

**Best practices:**
- Используй IF EXISTS / IF NOT EXISTS
- Создавай индексы CONCURRENTLY в отдельной миграции
- Избегай ALTER TABLE на больших таблицах
- Тестируй на копии данных

Создай файлы:
- migrations/YYYYMMDD_HHMMSS_$1_up.sql
- migrations/YYYYMMDD_HHMMSS_$1_down.sql
```

### 7. Оптимизация производительности

`.claude/commands/optimize.md`
```markdown
---
description: Анализ и оптимизация производительности
argument-hint: [file-path]
---

Файл для оптимизации: @$1

**Анализ:**

1. **Алгоритмическая сложность**
   - Найти O(n²) и выше
   - Оптимизировать циклы
   - Использовать правильные структуры данных

2. **Memory usage**
   - Утечки памяти
   - Ненужные копирования
   - Кеширование

3. **I/O операции**
   - Batch операции
   - Асинхронность
   - Connection pooling

4. **Database queries**
   - N+1 проблемы
   - Индексы
   - Eager loading

**Метрики:**
- Time complexity
- Space complexity
- Количество DB queries
- Response time

**Предложи оптимизации с benchmark результатами.**
```

### 8. Security audit

`.claude/commands/security-audit.md`
```markdown
---
description: Провести security аудит кода
argument-hint: [file-path]
---

Security аудит: @$1

**Проверить:**

### 1. Injection
- SQL injection
- NoSQL injection
- Command injection
- XSS
- LDAP injection

### 2. Authentication & Authorization
- Слабые пароли
- Session management
- JWT безопасность
- Access control

### 3. Sensitive Data
- Хранение паролей (hashing)
- Encryption at rest
- Encryption in transit
- API keys в коде

### 4. Security Misconfiguration
- Default credentials
- Verbose error messages
- Unnecessary services
- CORS настройки

### 5. Dependencies
- Vulnerable packages
- Outdated libraries

**Формат отчета:**

## 🔴 Critical
Критичные уязвимости требующие немедленного исправления

## 🟡 Medium
Средний приоритет

## 🟢 Low / Info
Рекомендации

Для каждой проблемы:
- Описание
- Потенциальный риск
- Как исправить (с кодом)
```

## Встроенные команды

Claude Code имеет встроенные команды:

| Команда | Описание |
|---------|----------|
| `/help` | Список всех команд |
| `/clear` | Очистить историю |
| `/compact [instructions]` | Сжать контекст |
| `/cost` | Показать использование токенов |
| `/memory` | Редактировать CLAUDE.md |
| `/model` | Выбрать модель |
| `/permissions` | Управление permissions |
| `/output-style` | Выбор стиля вывода |
| `/agents` | Управление субагентами |
| `/mcp` | Управление MCP серверами |
| `/sandbox` | Включить изоляцию |
| `/init` | Создать CLAUDE.md |
| `/config` | Настройки проекта |

## SlashCommand Tool

Claude может вызывать ваши команды программно через инструмент `SlashCommand`.

### Требования

1. Команда должна иметь `description` в frontmatter
2. Команда не должна быть встроенной
3. Не установлен `disable-model-invocation: true`

### Пример использования

**CLAUDE.md:**
```markdown
# Project Guidelines

## Testing
После написания нового кода всегда запускай /test для создания тестов.

## Commits
Используй /commit для создания conventional commit messages.
```

Claude автоматически вызовет эти команды когда нужно.

### Ограничение permissions

```json
{
  "permissions": {
    "deny": ["SlashCommand"]
  }
}
```

Или для конкретной команды:
```json
{
  "permissions": {
    "deny": ["SlashCommand:/commit"]
  }
}
```

## Лучшие практики

### 1. Одна задача — одна команда

❌ Плохо: `/do-everything`
✅ Хорошо: `/commit`, `/test`, `/docs`

### 2. Понятные имена

Используйте kebab-case:
- `/create-pr`
- `/review-code`
- `/deploy-staging`

### 3. Документируйте аргументы

Всегда используйте `argument-hint`:
```yaml
argument-hint: [pr-number] [priority]
```

### 4. Ограничивайте инструменты

Давайте только необходимые permissions:
```yaml
allowed-tools: Bash(git:*), Read
```

### 5. Используйте namespace

Организуйте по категориям:
```
.claude/commands/
├── git/
├── docs/
├── test/
└── deploy/
```

### 6. Добавляйте примеры

Включайте примеры использования в команду:
```markdown
Пример использования:
/review-pr 123 high alice
```

### 7. Версионируйте команды

Для breaking changes создавайте новые версии:
- `/deploy` — текущая версия
- `/deploy-v2` — новая версия

## Skills vs Commands

### Когда использовать Commands:

- ✅ Простые, часто используемые задачи
- ✅ Шаблоны промптов
- ✅ Быстрый доступ через `/`
- ✅ Инструкции в одном файле

### Когда использовать Skills:

- ✅ Сложные многошаговые процессы
- ✅ Требуется несколько файлов
- ✅ Скрипты и вспомогательные инструменты
- ✅ Автоматическая активация (не ручная)

## Troubleshooting

### Команда не найдена

**Проблема:** `/my-command` не распознается.

**Решения:**
1. Проверьте имя файла: `my-command.md`
2. Проверьте расположение: `.claude/commands/`
3. Перезапустите сессию Claude

### Команда не вызывается через SlashCommand tool

**Проблема:** Claude не может вызвать команду автоматически.

**Решения:**
1. Добавьте `description` в frontmatter
2. Убедитесь что не установлен `disable-model-invocation: true`
3. Проверьте permissions — не заблокирован ли `SlashCommand`

### Bash команды не выполняются

**Проблема:** `!git status` не работает.

**Решения:**
1. Добавьте `allowed-tools: Bash(git:*)`
2. Используйте правильный синтаксис: !`command`
3. Проверьте что команда доступна в PATH

### Аргументы не подставляются

**Проблема:** `$1` отображается как `$1` вместо значения.

**Решения:**
1. Передайте аргументы: `/command arg1 arg2`
2. Используйте `$ARGUMENTS` для всех аргументов
3. Убедитесь что используете правильный номер ($1, $2, $3)

## Расширенные техники

### Условная логика

```markdown
---
description: Deploy с выбором окружения
argument-hint: [environment]
---

Текущая ветка: !`git branch --show-current`

Деплой в окружение: $1

Если $1 == "production":
  1. Проверь что ветка main
  2. Запусти полный набор тестов
  3. Создай git tag
  4. Deploy с подтверждением

Если $1 == "staging":
  1. Запусти smoke tests
  2. Deploy автоматически

Если $1 == "dev":
  1. Deploy без тестов
```

### Цепочка команд

```markdown
Выполни последовательно:
1. /test $1 — создать тесты
2. Убедись что тесты проходят
3. /commit — создать коммит
4. /create-pr — создать PR
```

### Шаблоны с переменными

```markdown
---
description: Создать новый API endpoint
argument-hint: [resource-name]
---

Создай CRUD endpoint для: $1

**Файлы:**
- routes/$1.routes.js
- controllers/$1.controller.js
- models/$1.model.js
- tests/$1.test.js

Используй существующие endpoints как референс: @routes/users.routes.js
```

## Полезные ссылки

- 📚 [Официальная документация](https://docs.claude.com/en/docs/claude-code/slash-commands)
- 🔧 [Reference по командам](https://docs.claude.com/en/docs/claude-code/reference/slash-commands)
- ⚙️ [Settings и permissions](https://docs.claude.com/en/docs/claude-code/settings)
- 🪝 [Hooks для автоматизации](https://docs.claude.com/en/docs/claude-code/hooks)

---

**Следующий шаг:** Изучите [Skills](./03-skills.md) для создания сложных возможностей агентов.
