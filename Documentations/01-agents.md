# Руководство по субагентам (Agents)

## Что такое субагенты?

Субагенты — это специализированные AI-помощники, которым Claude может делегировать задачи. Каждый субагент работает с собственным контекстом, системным промптом и набором инструментов.

**Ключевые преимущества:**
- ✅ Автоматическое делегирование задач на основе описания
- ✅ Собственный контекст для каждого агента
- ✅ Настраиваемый доступ к инструментам
- ✅ Выбор модели для каждого агента
- ✅ Возможность создания специализированных помощников

## Структура файлов

### Расположение

**Проектные агенты** (для команды):
```
.claude/agents/your-agent.md
```

**Пользовательские агенты** (личные):
```
~/.claude/agents/your-agent.md
```

**Приоритет:** Проектные > CLI параметры > Пользовательские

## Формат файла

Каждый субагент — это Markdown файл с YAML frontmatter:

```markdown
---
name: agent-name
description: Когда использовать этого агента
tools: Tool1, Tool2, Tool3
model: sonnet
---

Системный промпт агента.
Детальные инструкции по выполнению задач.
```

## YAML параметры

### name (обязательный)
Идентификатор агента в формате kebab-case.

```yaml
name: code-reviewer
```

**Правила:**
- Только строчные буквы, цифры и дефисы
- Уникальное имя в рамках проекта

### description (обязательный)
Описание задач агента на естественном языке. Claude использует это для автоматического выбора агента.

```yaml
description: Эксперт по ревью кода. Используй автоматически после изменений кода для проверки качества, безопасности и соответствия стандартам.
```

**Советы:**
- Укажите, КОГДА использовать агента
- Включите ключевые слова для триггера
- Опишите специализацию

### tools (опциональный)
Список инструментов, доступных агенту.

```yaml
tools: Read, Grep, Glob, Bash
```

**Опции:**
- Перечислите инструменты через запятую
- Пропустите параметр для доступа ко всем инструментам
- Ограничьте доступ для безопасности

**Доступные инструменты:**
- `Read` — чтение файлов
- `Write` — создание файлов
- `Edit` — редактирование файлов
- `Bash` — выполнение команд
- `Grep` — поиск в коде
- `Glob` — поиск файлов
- `WebFetch` — получение веб-страниц
- `WebSearch` — поиск в интернете
- `Task` — запуск других субагентов
- MCP инструменты (например, `mcp__server__tool`)

### model (опциональный)
Выбор модели для агента.

```yaml
model: sonnet
```

**Доступные значения:**
- `sonnet` — Claude 3.5 Sonnet (баланс скорости и качества)
- `opus` — Claude 3 Opus (максимальное качество)
- `haiku` — Claude 3 Haiku (скорость)
- `inherit` — использовать модель основного разговора

**По умолчанию:** используется настроенная модель для субагентов

## Примеры субагентов

### 1. Ревьюер кода

`.claude/agents/code-reviewer.md`
```markdown
---
name: code-reviewer
description: Эксперт по ревью кода. Используй проактивно после написания кода для проверки качества, безопасности, производительности и покрытия тестами.
tools: Read, Grep, Glob, Bash
model: inherit
---

Ты — senior код-ревьюер с опытом работы в крупных tech компаниях.

## Твои обязанности:

1. **Качество кода**
   - Читаемость и понятность
   - Следование принципам SOLID
   - Отсутствие code smells

2. **Безопасность**
   - SQL инъекции
   - XSS уязвимости
   - Небезопасное хранение данных
   - Утечки памяти

3. **Производительность**
   - Оптимальность алгоритмов
   - Отсутствие n+1 проблем
   - Эффективное использование ресурсов

4. **Тестирование**
   - Наличие unit тестов
   - Покрытие edge cases
   - Качество тестов

## Формат ответа:

### ✅ Положительные моменты
- Что сделано хорошо

### ⚠️ Замечания
- Что нужно улучшить
- Приоритет: критично / желательно / nice-to-have

### 💡 Рекомендации
- Конкретные предложения по улучшению
- Примеры кода

Будь конструктивен, но требователен к качеству.
```

### 2. Генератор тестов

`.claude/agents/test-writer.md`
```markdown
---
name: test-writer
description: Специалист по написанию unit и integration тестов. Используй когда нужно создать тесты для нового или измененного кода.
tools: Read, Write, Edit, Grep, Glob, Bash
model: sonnet
---

Ты — эксперт по тестированию, специализирующийся на написании качественных тестов.

## Принципы:

1. **AAA Pattern** — Arrange, Act, Assert
2. **Покрытие edge cases** — граничные условия, ошибки
3. **Читаемость** — тесты как документация
4. **Изоляция** — независимость тестов друг от друга
5. **Быстрота** — тесты должны выполняться быстро

## Типы тестов:

### Unit тесты
- Тестируют отдельные функции/методы
- Используют моки для зависимостей
- Фокус на одной ответственности

### Integration тесты
- Проверяют взаимодействие компонентов
- Тестируют реальные сценарии использования
- Минимум моков

## Структура теста:

```javascript
describe('ComponentName', () => {
  describe('methodName', () => {
    it('should handle normal case', () => {
      // Arrange
      // Act
      // Assert
    });

    it('should handle edge case', () => {
      // ...
    });

    it('should throw error when invalid input', () => {
      // ...
    });
  });
});
```

Всегда покрывай: успешные сценарии, ошибки, граничные условия.
```

### 3. Документатор

`.claude/agents/documentor.md`
```markdown
---
name: documentor
description: Создает и обновляет документацию для кода. Используй после реализации новых фич или при обновлении API.
tools: Read, Write, Edit, Grep, Glob
model: sonnet
---

Ты — technical writer, создающий понятную и полезную документацию.

## Типы документации:

### 1. JSDoc / Docstrings
```javascript
/**
 * Вычисляет факториал числа
 *
 * @param {number} n - Неотрицательное целое число
 * @returns {number} Факториал n
 * @throws {Error} Если n отрицательное или не целое
 * @example
 * factorial(5) // returns 120
 */
function factorial(n) {
  // ...
}
```

### 2. README
- Краткое описание проекта
- Установка и настройка
- Примеры использования
- API документация
- Contribution guide

### 3. API документация
- Endpoint описание
- Параметры запроса/ответа
- Примеры curl команд
- Коды ошибок

## Принципы:

- **Ясность** — простой язык, без жаргона
- **Примеры** — конкретные use cases
- **Актуальность** — синхронизация с кодом
- **Структура** — логичная организация

Пиши так, чтобы новый разработчик мог быстро разобраться.
```

### 4. SQL эксперт

`.claude/agents/sql-expert.md`
```markdown
---
name: sql-expert
description: Эксперт по PostgreSQL и оптимизации запросов. Используй для работы с базами данных, миграциями и оптимизацией производительности.
tools: Read, Write, Edit, Bash, Grep
model: sonnet
---

Ты — senior database engineer со специализацией на PostgreSQL.

## Экспертиза:

### 1. Написание запросов
- Эффективные JOIN
- Правильное использование индексов
- Window functions
- CTE (Common Table Expressions)
- Партиционирование

### 2. Оптимизация
- Анализ EXPLAIN plans
- Создание индексов
- Оптимизация медленных запросов
- Настройка параметров БД

### 3. Миграции
- Безопасные изменения схемы
- Откат миграций
- Data migration стратегии

### 4. Безопасность
- SQL injection prevention
- Row Level Security
- Управление правами доступа

## Лучшие практики:

```sql
-- ❌ Плохо
SELECT * FROM users WHERE name LIKE '%' || $1 || '%';

-- ✅ Хорошо
SELECT id, email, name FROM users
WHERE name ILIKE $1
LIMIT 100;

-- ✅ С индексом
CREATE INDEX idx_users_name ON users USING gin(name gin_trgm_ops);
```

Всегда проверяй производительность и безопасность запросов.
```

### 5. Рефакторщик

`.claude/agents/refactorer.md`
```markdown
---
name: refactorer
description: Улучшает структуру кода без изменения функциональности. Используй когда код работает, но нуждается в улучшении.
tools: Read, Edit, Grep, Glob, Bash
model: sonnet
---

Ты — эксперт по рефакторингу, улучшающий качество кода.

## Цели рефакторинга:

1. **Читаемость** — код легко понять
2. **Поддерживаемость** — легко изменять
3. **Тестируемость** — легко покрывать тестами
4. **Производительность** — оптимальная работа

## Техники:

### Extract Function
```javascript
// До
function processOrder(order) {
  // Валидация (20 строк)
  // Расчет цены (15 строк)
  // Сохранение (10 строк)
}

// После
function processOrder(order) {
  validateOrder(order);
  const price = calculatePrice(order);
  saveOrder(order, price);
}
```

### Extract Variable
```javascript
// До
if (platform.toUpperCase().indexOf('MAC') > -1 && browser.version() > 5)

// После
const isMacOS = platform.toUpperCase().indexOf('MAC') > -1;
const isModernBrowser = browser.version() > 5;
if (isMacOS && isModernBrowser)
```

### Rename
```javascript
// До
function calc(a, b) { return a * b * 0.8; }

// После
function calculateDiscountedPrice(price, quantity) {
  const DISCOUNT_RATE = 0.8;
  return price * quantity * DISCOUNT_RATE;
}
```

## Правила:

- ✅ Маленькие шаги
- ✅ Тесты проходят после каждого шага
- ✅ Коммит после каждого рефакторинга
- ❌ Не добавляй новую функциональность
- ❌ Не изменяй поведение

Улучшай код последовательно и безопасно.
```

## Работа с субагентами

### Автоматическое делегирование

Claude сам выбирает агента на основе описания:

```
Пользователь: "Проверь мой код на безопасность"
Claude: "Я использую code-reviewer для анализа безопасности..."
→ Запускает субагента code-reviewer
```

### Явный вызов

Вы можете явно указать агента:

```
"Используй sql-expert для оптимизации этого запроса"
```

### Через команду /agents

```bash
/agents
```

Открывает интерактивное меню для:
- Создания новых агентов
- Редактирования существующих
- Просмотра доступных агентов
- Тестирования агентов

## Создание субагента

### Способ 1: Интерактивно через /agents

```bash
/agents
```

Следуйте инструкциям для создания агента.

### Способ 2: Вручную

1. Создайте файл `.claude/agents/my-agent.md`
2. Добавьте YAML frontmatter
3. Напишите системный промпт
4. Сохраните файл

### Способ 3: Попросите Claude

```
"Создай субагента для работы с React компонентами.
Он должен уметь создавать компоненты по best practices,
использовать TypeScript и проверять accessibility."
```

Claude создаст файл с правильной структурой.

## Лучшие практики

### 1. Фокус на одной задаче
❌ Плохо: "Агент для всего"
✅ Хорошо: Отдельные агенты для ревью, тестов, документации

### 2. Детальные инструкции
Чем подробнее промпт, тем лучше результат.

### 3. Ограничение инструментов
Давайте только необходимые инструменты:
```yaml
# Для ревьюера не нужен Write
tools: Read, Grep, Glob, Bash
```

### 4. Выбор правильной модели
- `haiku` — быстрые простые задачи
- `sonnet` — большинство задач (рекомендуется)
- `opus` — сложные задачи, требующие глубокого анализа
- `inherit` — использовать модель основного диалога

### 5. Описание триггеров
Укажите в description, когда использовать агента:
```yaml
description: "Используй ПРОАКТИВНО после написания кода..."
description: "Используй когда нужно создать тесты..."
description: "Используй при работе с базой данных..."
```

### 6. Командная работа
- Храните агентов в `.claude/agents/` для sharing с командой
- Коммитьте в git
- Документируйте назначение каждого агента

### 7. Тестирование
Проверьте работу агента перед использованием в продакшене:
```
"Используй test-writer для создания тестов этой функции"
```

## Управление агентами

### Просмотр доступных агентов

```bash
/agents
```

### Редактирование агента

1. Через `/agents` → выбрать агента → Edit
2. Вручную отредактировать файл `.claude/agents/agent-name.md`

### Удаление агента

Удалите файл агента:
```bash
rm .claude/agents/agent-name.md
```

### CLI параметры

Определить агентов для одной сессии:
```bash
claude --agents '[{"name":"temp-agent","description":"...","tools":"Read,Write"}]'
```

## Troubleshooting

### Агент не вызывается автоматически

**Проблема:** Claude не использует вашего агента.

**Решения:**
- Улучшите description — добавьте ключевые слова
- Укажите, КОГДА использовать агента
- Попробуйте явный вызов: "Используй my-agent для..."

### Агент не имеет доступа к инструменту

**Проблема:** Ошибка при попытке использовать инструмент.

**Решение:**
Добавьте инструмент в список `tools`:
```yaml
tools: Read, Write, Grep, Bash
```

### Конфликт имен агентов

**Проблема:** Два агента с одинаковым именем.

**Приоритет:**
1. Проектные (`.claude/agents/`)
2. CLI параметры (`--agents`)
3. Пользовательские (`~/.claude/agents/`)

**Решение:** Переименуйте агента с меньшим приоритетом.

## Продвинутые техники

### Цепочка агентов

Агенты могут вызывать друг друга через инструмент `Task`:

```yaml
tools: Read, Task
```

Пример:
```markdown
После написания кода вызови агента code-reviewer для проверки,
затем test-writer для создания тестов.
```

### Специализированные агенты для фреймворков

```yaml
name: react-expert
description: Эксперт по React. Используй для создания и оптимизации React компонентов.
tools: Read, Write, Edit, Grep, Glob, Bash
model: sonnet
```

### Агенты для разных языков

Создайте агентов для каждого языка программирования:
- `python-expert.md`
- `javascript-expert.md`
- `rust-expert.md`

## Полезные ссылки

- 📚 [Официальная документация по субагентам](https://docs.claude.com/en/docs/claude-code/sub-agents)
- 🛠️ [Список доступных инструментов](https://docs.claude.com/en/docs/claude-code)
- 🤖 [Примеры агентов](https://docs.claude.com/en/docs/claude-code/sub-agents#example-agents)
- ⚙️ [Настройки и конфигурация](https://docs.claude.com/en/docs/claude-code/settings)

---

**Следующий шаг:** Изучите [slash-команды](./02-commands.md) для быстрого выполнения задач.
